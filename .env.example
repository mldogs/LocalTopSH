# ============================================
# LocalTopSH Configuration
# ============================================
# For Docker Compose: copy to .env (gitignored)
# Secrets are injected into the `proxy` container only.
# ============================================

# LLM API (example: OpenRouter)
BASE_URL=https://openrouter.ai/api/v1
MODEL_NAME=google/gemini-3-flash-preview
API_KEY=sk-your-api-key

# Telegram Bot
TELEGRAM_TOKEN=your-telegram-bot-token
ALLOWED_USERS=123456789

# Web search (set at least one)
# Tavily (preferred): https://tavily.com/
TAVILY_API_KEY=tvly-your-tavily-api-key
# Z.AI (fallback): https://z.ai/
ZAI_API_KEY=your-zai-api-key

# Gateway settings
MCP_PORT=3100
MCP_URL=http://localhost:3100
WORKSPACE=/workspace
AGENT_CWD=/workspace
BOT_PROFILE=october

# Network - port range for user services
EXPOSED_PORTS=4000-4999

# Max concurrent users
MAX_CONCURRENT_USERS=10

# Proxy URL inside Docker Compose (usually keep default)
PROXY_URL=http://proxy:3200
